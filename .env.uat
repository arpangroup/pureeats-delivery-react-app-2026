# Live backend environment — talks to the real pureeats-backend-2026 Spring
# Boot app running locally, same one the customer/admin apps point at in
# their own .env.uat. Run with:
#   npm run dev:uat
VITE_DATA_SOURCE=live
VITE_API_BASE_URL=http://localhost:8081/api/v1
VITE_MOCK_DELAY_MS=0
